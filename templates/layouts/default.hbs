<!DOCTYPE html>
<html lang="en" class="{{theme}}-theme">
<head>
    {{> head}}
</head>
<body>
    <div class="container">
        {{{content}}}
    </div>

    {{> tooltip}}
    {{> scripts}}
</body>
</html>